// Testing log
console.log("typescript tutorial");

// 1. Basics
// Declare interface
interface someValue {
  name: string;
  id: number;
}

// Create object with assigned interface
let someObj: someValue = {
  name: "random",
  id: 123,
};

// Log the object
console.log(someObj);
