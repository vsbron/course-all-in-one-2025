import {
  CLEAR_CART,
  REMOVE,
  INCREASE,
  DECREASE,
  LOADING,
  DISPLAY_ITEMS,
} from "./action";

// Create reducer
const reducer = (state, action) => {
  return state;
};

export default reducer;
